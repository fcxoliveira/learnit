<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ufrpe.edu.learnit">

    <!-- To auto-complete the email text field in the login form with the usuario's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".usuario.gui.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".usuario.gui.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".usuario.gui.HomeActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".perfil.gui.PerfilActivity" />
        <activity
            android:name=".usuario.gui.InteressesActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".perfil.gui.EditarPerfilActivity" />
        <activity
            android:name=".aula.gui.ComprarAulaAlunoActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".aula.gui.AulasOferecidasActivity"/>
        <activity android:name=".aula.gui.AulasCompradasActivity"/>
        <activity android:name=".usuario.gui.CoinsActivity"/>
        <activity android:name=".aula.gui.CadastrarAulaTutorActivity"/>
    </application>

</manifest>